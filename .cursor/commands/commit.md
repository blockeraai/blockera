# commit

You are in chat and we made changes into code base in this chat and we want to commit it.

Check the chat and if there was a final commit subject and message the changes use it and commit it to git.

If there wasn't commit subject and message. generate and show it to use and if accepts it, then commit to git. if user made changes and send the commit subject and message then commit that.

If there is changes that are not exactly related or can be separated, then commit them separately in different commits. I mean micro commit is more suitable if the changes are not related or depended.

In the each packages of blockera we have CHANGELOG.md file that we add the changelog for user. If the commit has changes that we have to inform that to end users of product, then we have to add a changelog to inform to users. Only changes that are usefull for end users shluld be added and the changelog message should follow the file pattern and also writen a way that end user can understand what that does.
