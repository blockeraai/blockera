# Ignore artifacts:
dist
example
vendor
source-code-block-editor
source-code-wordpress

# Fixture HTML uses the built-in HTML formatter (Prettier rewrites inline CSS spacing).
tests/**/*.html
tests/**/*.css
