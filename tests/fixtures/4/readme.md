**blockera-block-1**: image block + The inner img have custom styles + has `caption` + custom style for the `caption` form block.

**blockera-block-2**: attribute that uses custom css value type (func).

**blockera-block-3**: attribute that uses custom css value type (func).

**blockera-block-4**: attribute that uses custom css value type (func).
