**blockera-block--1**: block has `id` attribute and it shouldn't be removed in the HTML and not used in CSS selector.

**blockera-block--2**: block has `custom-class` class and it should be available in the HTML and not to be used in CSS selector.
