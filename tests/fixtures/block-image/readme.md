**blockera-block-1**: image block. The inner img have custom styles.

**blockera-block-2**: image block. The inner img have custom styles (complex styles).

**blockera-block-3**: image block + The inner img have custom styles + has `caption` + custom style for the `caption` form block.

**blockera-block-4**: inline `img` element with custom `style`.

