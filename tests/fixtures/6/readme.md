**blockera-block-1**: inner `code` element.

**blockera-block-2**: inner `kbd` element.

**blockera-block-3**: inner `bdo` element (have custom attrs but not classname).

**blockera-block-4**: inner `math` element (complex inner html but no any Blockera customization on them).
