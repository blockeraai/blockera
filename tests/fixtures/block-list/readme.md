**blockera-block-1**: `List` block with icon and gap + `List Item` inner block style.

**blockera-block-2**: `column` block have width that should be `flex-basis` and have `blockera-has-flex-basis` class.
