**blockera-block-1**: Custom colors used for styling root and `mark` inner element

**blockera-block-2**: Variable colors used for styling root and `mark` inner element.

**blockera-block-3**: No customization for `mark` inner element.
