{
	"$id": "https://blockera.ai/snapshot-tests-config.schema.json",
	"title": "Snapshot Tests Config",
	"description": "The configuration for snapshot tests.",
	"type": "object",
	"properties": {
		"screenshot": {
			"type": "boolean",
			"default": true,
			"description": "Whether to take a screenshot of the block."
		},
		"snapshot": {
			"type": "boolean",
			"default": true,
			"description": "Whether to take a snapshot (html and CSS) of the block."
		}
	}
}