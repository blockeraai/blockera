// @flow

/**
 * Identifies which Blockera global-styles panel surface is active (parallel UI stacks).
 */
export const VARIATION_SURFACE_STYLE = 'style';
export const VARIATION_SURFACE_SIZE = 'size';

/**
 * Slot prefix for Size Variations panel — avoids SlotFill clashes with Style Variations.
 */
export const SIZE_VARIATION_BLOCK_CARD_SLOT_NAME =
	'blockera-size-variation-block-card';
